services:
  - type: web
    name: friendify-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TOKEN
        sync: false
      - key: HF_TOKEN
        sync: false
