# Flask for optional web routes (can keep or remove)
Flask==2.3.3

# Telegram bot with webhook support
python-telegram-bot[webhooks]==20.3

# HTTP client for async requests (used by OpenAI or other APIs)
httpx==0.24.1

# OpenAI API client
openai==1.30.0

# Optional utilities
requests==2.31.0
tqdm==4.67.1

# Dependencies for pydantic or data validation if needed
pydantic==2.12.3
pydantic-core==2.41.4

# Networking / async support
anyio==4.11.0
sniffio==1.3.1
typing_extensions==4.15.0
